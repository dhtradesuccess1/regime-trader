"""Settings package for regime_trader.

Exposes configuration constants (``settings.config``) and the tradable
universe definition (``settings.tickers``). Application code imports trading
parameters and risk limits from here rather than hard-coding them.
"""
